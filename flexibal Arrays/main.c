#include <stdio.h>
#include <stdlib.h>
// struct FArray
// {
//     unsigned int size;
//     int array[];
// };
// typedef struct FArray FArray;

// int main(void)
// {
//     FArray fArray;
//     printf("enter the size of arrays :" );
//     scanf("%d",&fArray.size);
//     fArray = malloc(sizeof(FArray)+sizeof(int)*fArray.size);

// }
int main(void)
{
#if defined __STDC_NO_COMPLEX__
#else
    printf("complex arithmetic supported!\n");
#include <complex.h>
#if defined i
#undef i
    printf("i is defined\n");
#endif

    complex double Temp;

#endif
}